{
  "name": "fieldcraft-digital",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w shared && npm run build -w frontend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "clean": "rm -rf frontend/.next frontend/node_modules backend/node_modules shared/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "typescript": "^5.9.3"
  }
}
